// The remainder operator in javascript is not the same as the modulus operator since it doesn't always give accurate results with negative numbers.

// Only change code below this line

var remainder;

remainder = 11 % 3;