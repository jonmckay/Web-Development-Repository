// The strict inequality operator is the logical opposite of the strict equality operator. There is no type conversion done.

// Setup
function testStrictNotEqual(val) {
    // Only Change Code Below this Line
    
    if (val !== 17) {
  
    // Only Change Code Above this Line
  
      return "Not Equal";
    }
    return "Equal";
  }
  
  // Change this value to test
  testStrictNotEqual(10);