// Create a function with two arguments and add them together. Pass the function two numbers.

// Example
function ourFunctionWithArgs(a, b) {
    console.log(a - b);
  }
  ourFunctionWithArgs(10, 5); // Outputs 5
  
  // Only change code below this line.
  function functionWithArgs(num, num2) {
    console.log(num + num2);
  }
  
  functionWithArgs(1, 2);
  